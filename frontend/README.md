# Frontend (berribot-interview)

This frontend is built with Vite + React.

Run locally:
```powershell
cd frontend
npm install
npm run dev
```

It expects a backend running at `http://localhost:8000`. Configure via `.env` (VITE_API_URL).
